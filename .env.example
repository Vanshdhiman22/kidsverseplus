# Public API origin only: never put database credentials or private keys in a Vite env file.
VITE_API_MODE=mock
VITE_API_BASE_URL=/api/v1

# For a separately approved live test, set VITE_API_MODE=live and the backend's /api/v1 URL.
# Mock server only exists in npm run dev; do not deploy this configuration.
# VITE_LEARNING_PACKAGE_ID=addition-introduction
